/**
 * 网络安全断言（Cybersecurity Claim）
 * @property {string} name - 断言名称
 * @property {string} label - 断言标签
 * @property {string} description - 断言描述
 */
export declare interface CybersecurityClaim {
    name: string;
    label: string;
    description: string;
}
